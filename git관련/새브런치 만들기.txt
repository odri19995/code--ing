예시 집에서 쓰는 브런치 만들기

먼저 브런치를 만드려면 commit 이 있어야한다. 아니면 브런치가 만들어지지 않는다. 

$ git commit -m "home commit"


$ git branch myhome
my home 이라는 브런치를 만들었다. 

$ git branch
브런치 확인

$ git switch myhome
myhome 브런치 이동


$ git push origin myhome
올리기
